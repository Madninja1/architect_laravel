# Laravel Architecture Sandbox (DDD / SOLID / Clean Architecture / R&D Playground)

> Этот репозиторий — **песочница для архитектурных практик** на Laravel.  
> Он создан в учебных и R&D целях, чтобы продемонстрировать навыки senior Laravel developer / software engineer / архитектора.

## 🎯 Цель проекта

Показать на реальных примерах:

- как применять **DDD (Domain-Driven Design)** в Laravel-монолите;
- как разрабатывать модули в стиле **clean architecture**;
- как организовывать код так, чтобы он был:
    - расширяемым,
    - тестируемым,
    - слабосвязанным,
    - читаемым,
    - понятным для команды;
- как использовать **SOLID, DRY, KISS** в реальных bounded contexts;
- как строить **слои**, **модули**, **контексты**, **domain events**, **application services**, **repository abstractions** и т.д.

Проект не предназначен как production-биллинг, CRM или marketplace.  
Это **архитектурная демонстрация**.

---

## 🧱 Общая концепция

Каждая доменная область (bounded context):

- живёт в своей директории вида `app/<ContextName>`;
- имеет собственную внутреннюю архитектуру:
    - **Domain**
    - **Application**
    - **Infrastructure**
    - **Interfaces (Delivery Layer)**
- остаётся максимально автономной.

Пример контекстов:

- `Billing`
- `Inventory`
- `UserAccess`
- `Notifications`
- `Analytics`

> Контексты создаются постепенно в ветках как отдельные модули.  
> Каждая ветка иллюстрирует определённый аспект архитектуры.

---

## 🌿 Git Flow (ветки и их назначение)

Мы используем упрощённый Git Flow:

- `main` — продуктивная ветка.
- `pre-prod` - stage ветка для предрелизного тестирования.
- `develop` — интеграционная ветка.
- `feature/*` — фичи/задачи (ответвляются от `main`, мержатся в `develop`).
- `release/*` — подготовка релизов (ответвляются от `main`, мержатся в `preprod` и `main`).
- `hotfix/*` — срочные фиксы продакшна (ответвляются от `main`, мержатся в `main`, `preprod` и `develop`).

Жизненный цикл:

1. Задача → `feature/*` от `main`.
2. По завершении → merge в `develop`.
3. Когда набор задач готов к релизу → `release/x.y.z` от `main`.
4. Release деплоится на pre-prod, правятся баги.
5. После одобрения:
    - merge `release/x.y.z` → `main` (+ tag).
6. При срочном баге в проде:
    - `hotfix/*` от `main`,
    - merge в `main`, `preprod` и `develop`.

---

## 📦 Используемые архитектурные принципы

### ✔ Domain-Driven Design

- Явное выделение bounded contexts.
- Domain Model без Laravel-зависимостей.
- Domain Events.
- Repository interfaces + Infrastructure implementations.

### ✔ Clean Architecture / Hexagonal Architecture

- Чёткие слои: Domain → Application → Infrastructure → Interface.
- Внешние слои знают о внутренних, но не наоборот.
- В домене нет Eloquent, фасадов, запросов Laravel и т.д.

### ✔ SOLID

- SRP — каждый класс делает одно.
- OCP — новые способы оплаты добавляются новыми gateway.
- DIP — слой Application зависит только от интерфейсов.

### ✔ DRY / KISS

- Не дублируем бизнес-логику в HTTP-слое.
- Простые, предсказуемые структуры.
- Никакой «магии» фреймворка внутри Domain.

---

## 📁 Структура проекта (базовая)

